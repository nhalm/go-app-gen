package api

import (
	"github.com/go-chi/chi/v5"
	"github.com/go-chi/chi/v5/middleware"
)

// RegisterRoutes registers all API routes
func RegisterRoutes(r chi.Router, handler *{{.DomainTitle}}Handler) {
	// Health check endpoint
	r.Get("/health", handler.HealthCheck)
	
	// API version group
	r.Route("/api/v1", func(r chi.Router) {
		// Add middleware for API routes
		r.Use(middleware.SetHeader("Content-Type", "application/json"))
		
		// {{.DomainTitle}} routes
		r.Route("/{{.DomainPlural}}", func(r chi.Router) {
			// Add your {{.Domain}} routes here
			// r.Get("/", handler.List{{.DomainTitle}}s)
			// r.Post("/", handler.Create{{.DomainTitle}})
			// r.Route("/{id}", func(r chi.Router) {
			//     r.Get("/", handler.Get{{.DomainTitle}})
			//     r.Put("/", handler.Update{{.DomainTitle}})
			//     r.Delete("/", handler.Delete{{.DomainTitle}})
			// })
		})
	})
}